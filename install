#!/usr/bin/env bash

set -e

BASEDIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if command -v dotbot &> /dev/null; then
    dotbot -d "${BASEDIR}" -c install.conf.json "${@}"
else
    echo "dotbot not found. Install with: pipx install dotbot"
    exit 1
fi
